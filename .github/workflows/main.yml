name: Manual Workflow

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Hello there"

      - name: Step 2
        run: date

  job2:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: echo "Yes bro, what's up?"

      - name: Step 2
        run: ls -la
